'use client';

export const Sep = () => <div className="h-px bg-border" />;

export const SectionLabel = ({ children }) => (
  <div className="text-[11px] font-semibold text-muted uppercase tracking-[0.06em]">{children}</div>
);
